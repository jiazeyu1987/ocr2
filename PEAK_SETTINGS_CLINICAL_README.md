`settings`里与峰值识别相关的主要是两组参数：`offline_peak` 和 `peak_detect`。`offline_peak` 用来控制治疗后图片在什么时刻截取，其中 `threshold` 控制对峰值变化的敏感度，`after_delay_frames` 控制在峰值结束后再延迟几帧取图，`end_diff_threshold` 控制系统多早认为峰值已经回落。`peak_detect` 用来控制最终结果判定，其中 `difference_threshold` 决定系统判“成功/失败”的严格程度，数值越小越容易判成功，越大越严格。其他如 `roi2_extension_params`、`roi3_extension_params` 和 override 参数属于辅助修正区域参数。
现在最终判定“HEM”的条件有 3 个，满足任意一个都可以，所以是“或”的关系。第 1 个条件是 peak_detect.difference_threshold，表示 ROI2 的前后差值达到阈值就判HEM。第 2 个条件在 peak_detect.roi3_g1_g2_override，当 g1_threshold 和 g2_threshold 同时满足时，可把原本的判定失败改成HEM。第 3 个条件在 peak_detect.roi3_column_diff_override，当 g1_threshold 和 threshold（列差阈值）同时满足时，也可把原本的判定失败改成HEM。对应都在 settings 的 peak_detect 下面。